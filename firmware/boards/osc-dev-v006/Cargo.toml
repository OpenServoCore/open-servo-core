[workspace]
resolver = "2"
members  = ["boot", "app"]

# Task #11 (half-duplex shared-wire fixes, OpenServoCore/tinyboot#32) rides
# the local checkout until a tinyboot release tags it — cargo can't patch a
# git dep from the same git URL. Drop this patch and bump the dep tag when
# the release lands.
[patch."https://github.com/OpenServoCore/tinyboot.git"]
tinyboot-ch32 = { path = "/Users/aaron/projects/OpenServoCore/tinyboot/ch32" }

[workspace.package]
edition = "2024"

[profile.dev]
opt-level     = 3
lto           = true
codegen-units = 1
debug         = 2
debug-assertions = false

# Bootloader is flash-size-bound and only runs once at startup; size beats speed.
[profile.dev.package.osc-dev-v006-boot]
opt-level = "z"

[profile.release]
opt-level     = 3
lto           = true
codegen-units = 1
panic         = "abort"
debug         = 2

[profile.release.package.osc-dev-v006-boot]
opt-level = "z"
