[package]
name    = "control-table"
version = "0.0.0"
edition.workspace = true
license.workspace = true

[dependencies]
heapless.workspace = true
defmt              = { version = "1", optional = true }

[features]
default = []
defmt   = ["dep:defmt"]
