[package]
name    = "osc-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true

[dependencies]
heapless.workspace         = true
bbqueue.workspace          = true
embedded-storage.workspace = true
portable-atomic.workspace  = true
critical-section.workspace = true
bitflags.workspace         = true
defmt                      = { version = "1", optional = true }

osc-units.workspace     = true
dxl-protocol            = { workspace = true, features = ["osc"] }
control-table.workspace = true

[dev-dependencies]
dxl-protocol = { workspace = true, features = ["heapless", "osc"] }

[features]
default = []
defmt   = ["dep:defmt", "control-table/defmt"]
# Mirrors the chip-side fire-path selection so `ConfigComms` can flip the
# TX-latency CT fields between RW and Reserved at the same byte offsets.
# Set by the chip crate's matching feature; not meant to be enabled alone.
dxl-hw-fire = []
